---
phase: 09-portal-admin-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web-portal/app/globals.css
  - apps/web-portal/components/ui/button.tsx
  - apps/web-portal/components/ui/card.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard background uses warm cream color (#FFFDF7)"
    - "Gradient buttons display blue-to-indigo gradient with hover lift effect"
    - "Cards lift with subtle shadow on hover"
  artifacts:
    - path: "apps/web-portal/app/globals.css"
      provides: "Brand color CSS variables in OKLCH format"
      contains: "--gradient-start"
    - path: "apps/web-portal/components/ui/button.tsx"
      provides: "Gradient button variant"
      contains: "variant.*gradient"
    - path: "apps/web-portal/components/ui/card.tsx"
      provides: "Card hover effects"
      contains: "hover:"
  key_links:
    - from: "button.tsx"
      to: "globals.css"
      via: "Tailwind gradient classes"
      pattern: "from-gradient-start.*to-gradient-end"
---

<objective>
Implement brand theming with warm cream background and blue-indigo gradient accents.

Purpose: Establish the visual foundation for the dashboard with consistent brand colors that will be used across all components.
Output: Updated CSS variables, gradient button variant, and card hover effects.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-portal-admin-ux-polish/09-RESEARCH.md
@apps/web-portal/app/globals.css
@apps/web-portal/components/ui/button.tsx
@apps/web-portal/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brand color variables to globals.css</name>
  <files>apps/web-portal/app/globals.css</files>
  <action>
Update the CSS theme configuration to add brand-specific colors:

1. In `:root` section, add brand color variables:
   - `--background: oklch(0.995 0.005 85);` (warm cream #FFFDF7)
   - `--gradient-start: oklch(0.55 0.18 250);` (blue)
   - `--gradient-end: oklch(0.45 0.22 280);` (indigo)

2. In `@theme inline` section, expose gradient colors:
   - `--color-gradient-start: var(--gradient-start);`
   - `--color-gradient-end: var(--gradient-end);`

3. In `.dark` section, add dark mode gradient colors:
   - `--gradient-start: oklch(0.60 0.18 250);` (slightly lighter for dark mode)
   - `--gradient-end: oklch(0.50 0.22 280);`

Note: The warm cream background will now apply site-wide through the existing `body { @apply bg-background }` rule.
  </action>
  <verify>
Run `cd /Users/christopherbartolo/Documents/GitHub/slack-speak-for-me/apps/web-portal && npm run build` to ensure CSS compiles without errors.
  </verify>
  <done>
CSS variables for warm cream background and blue-indigo gradient are defined in both light and dark modes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add gradient button variant</name>
  <files>apps/web-portal/components/ui/button.tsx</files>
  <action>
Add a new `gradient` variant to the buttonVariants cva configuration:

In the `variants.variant` object, add after `ghost`:

```
gradient:
  "bg-gradient-to-r from-gradient-start to-gradient-end text-white shadow-sm hover:shadow-lg hover:scale-[1.02] transition-all duration-300"
```

This creates:
- Blue-to-indigo gradient background using the new CSS variables
- White text for contrast
- Subtle shadow that increases on hover
- Slight scale-up (1.02) on hover for "lift" effect
- Smooth 300ms transition for all changes

The gradient uses Tailwind's built-in gradient utilities with the custom `--color-gradient-*` variables exposed in globals.css.
  </action>
  <verify>
Build succeeds: `cd /Users/christopherbartolo/Documents/GitHub/slack-speak-for-me/apps/web-portal && npm run build`
  </verify>
  <done>
Button component has `variant="gradient"` available that displays blue-indigo gradient with hover lift effect.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add hover effects to Card component</name>
  <files>apps/web-portal/components/ui/card.tsx</files>
  <action>
Update the Card function to add hover effects for interactive cards:

1. Modify the Card component className to add:
   - `transition-all duration-300` for smooth transitions
   - `hover:shadow-md hover:-translate-y-0.5` for lift effect on hover

The full className should become:
```
"bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm transition-all duration-300 hover:shadow-md hover:-translate-y-0.5"
```

Note: Using transform (`-translate-y-0.5`) and opacity transitions is GPU-accelerated for smooth performance. The shadow transition is acceptable at md level without performance issues.

Do NOT modify CardHeader, CardTitle, CardDescription, CardAction, CardContent, or CardFooter - only the Card component itself.
  </action>
  <verify>
Build succeeds: `cd /Users/christopherbartolo/Documents/GitHub/slack-speak-for-me/apps/web-portal && npm run build`
  </verify>
  <done>
Card component has hover effect that lifts cards slightly with increased shadow on hover.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors in web-portal
2. CSS variables are properly defined (check globals.css has --gradient-start and --gradient-end)
3. Button component exports `gradient` variant
4. Card component has hover transition classes
</verification>

<success_criteria>
- globals.css contains warm cream background (`oklch(0.995 0.005 85)`) and gradient color variables
- Button component has gradient variant with blue-indigo gradient
- Card component lifts on hover with shadow transition
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-portal-admin-ux-polish/09-01-SUMMARY.md`
</output>
