---
phase: 11-individual-billing
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - apps/web-portal/app/pricing/page.tsx
  - apps/web-portal/components/pricing/pricing-table.tsx
autonomous: true

must_haves:
  truths:
    - "Pricing page shows toggle between Individual and Team billing modes"
    - "Individual plans show per-user pricing without /seat/month"
    - "Team plans show per-seat pricing with /seat/month"
    - "CTA links include mode parameter for checkout"
  artifacts:
    - path: "apps/web-portal/components/pricing/pricing-table.tsx"
      provides: "Billing mode toggle and plan display"
      contains: "billingMode"
    - path: "apps/web-portal/app/pricing/page.tsx"
      provides: "Pricing page with mode support"
      contains: "BillingModeToggle"
  key_links:
    - from: "apps/web-portal/components/pricing/pricing-table.tsx"
      to: "/login"
      via: "CTA links with mode parameter"
      pattern: "mode=individual|mode=organization"
---

<objective>
Update pricing page to show billing mode toggle between Individual and Team pricing.

Purpose: Users need to clearly see individual vs team pricing options. Individual users should see per-user pricing, teams should see per-seat pricing.
Output: Pricing page with toggle, appropriate plan cards for each mode.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-individual-billing/11-RESEARCH.md
@apps/web-portal/app/pricing/page.tsx
@apps/web-portal/components/pricing/pricing-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add billing mode toggle to pricing table</name>
  <files>apps/web-portal/components/pricing/pricing-table.tsx</files>
  <action>
Refactor pricing-table.tsx to support both individual and team modes:

```typescript
'use client';

import { useState } from 'react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

type BillingMode = 'individual' | 'team';

interface Plan {
  name: string;
  price: number;
  priceUnit: string;
  description: string;
  features: string[];
  popular?: boolean;
  cta: string;
  planId: string;
}

const INDIVIDUAL_PLANS: Plan[] = [
  {
    name: 'Starter',
    price: 10,
    priceUnit: '/month',
    description: 'For personal use across any workspace',
    features: [
      'AI response suggestions',
      'Watch up to 5 channels',
      'Copy to clipboard',
      'Basic refinement',
      'Email support',
    ],
    cta: 'Start Free Trial',
    planId: 'individual_starter',
  },
  {
    name: 'Pro',
    price: 15,
    priceUnit: '/month',
    description: 'Full power for power users',
    features: [
      'Everything in Starter',
      'Unlimited channels',
      'Style learning',
      'Weekly reports',
      'Priority support',
    ],
    popular: true,
    cta: 'Start Free Trial',
    planId: 'individual_pro',
  },
];

const TEAM_PLANS: Plan[] = [
  {
    name: 'Team Starter',
    price: 10,
    priceUnit: '/seat/month',
    description: 'Perfect for small teams',
    features: [
      'AI response suggestions',
      'Watch up to 5 channels',
      'Copy to clipboard',
      'Basic refinement',
      'Team admin controls',
      'Email support',
    ],
    cta: 'Start Free Trial',
    planId: 'starter',
  },
  {
    name: 'Team Pro',
    price: 15,
    priceUnit: '/seat/month',
    description: 'For teams who need the full power',
    features: [
      'Everything in Starter',
      'Unlimited channels',
      'Style learning',
      'Weekly reports',
      'Google Sheets integration',
      'Priority support',
    ],
    popular: true,
    cta: 'Start Free Trial',
    planId: 'pro',
  },
];

function CheckIcon() {
  return (
    <svg
      className="w-5 h-5 text-indigo-500 flex-shrink-0"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M5 13l4 4L19 7"
      />
    </svg>
  );
}

function BillingModeToggle({
  mode,
  onChange
}: {
  mode: BillingMode;
  onChange: (mode: BillingMode) => void;
}) {
  return (
    <div className="flex items-center justify-center mb-12">
      <div className="flex items-center gap-1 p-1 bg-gray-100 rounded-lg">
        <button
          onClick={() => onChange('individual')}
          className={`px-4 py-2 rounded-md text-sm font-medium transition-all ${
            mode === 'individual'
              ? 'bg-white shadow-sm text-gray-900'
              : 'text-gray-600 hover:text-gray-900'
          }`}
        >
          For Myself
        </button>
        <button
          onClick={() => onChange('team')}
          className={`px-4 py-2 rounded-md text-sm font-medium transition-all ${
            mode === 'team'
              ? 'bg-white shadow-sm text-gray-900'
              : 'text-gray-600 hover:text-gray-900'
          }`}
        >
          For My Team
        </button>
      </div>
    </div>
  );
}

function PricingCard({ plan, mode }: { plan: Plan; mode: BillingMode }) {
  const ctaHref = `/login?plan=${plan.planId}&mode=${mode}`;

  return (
    <Card
      className={`relative flex flex-col bg-white ${
        plan.popular
          ? 'border-indigo-500 border-2 shadow-xl shadow-indigo-500/10 scale-105'
          : 'border-gray-200 shadow-sm'
      }`}
    >
      {plan.popular && (
        <div className="absolute -top-3 left-1/2 -translate-x-1/2">
          <span className="bg-gradient-to-r from-blue-500 to-indigo-500 text-white text-xs font-semibold px-3 py-1 rounded-full">
            Most Popular
          </span>
        </div>
      )}

      <CardHeader className="text-center pb-2">
        <CardTitle className="text-2xl">{plan.name}</CardTitle>
        <p className="text-gray-500 text-sm mt-1">{plan.description}</p>
      </CardHeader>

      <CardContent className="flex flex-col flex-grow">
        <div className="text-center mb-6">
          <div className="flex items-baseline justify-center">
            <span className="text-4xl font-bold">${plan.price}</span>
            <span className="text-gray-500 ml-1">{plan.priceUnit}</span>
          </div>
        </div>

        <ul className="space-y-3 mb-8 flex-grow">
          {plan.features.map((feature) => (
            <li key={feature} className="flex items-start gap-3">
              <CheckIcon />
              <span className="text-gray-700">{feature}</span>
            </li>
          ))}
        </ul>

        <Link href={ctaHref} className="block">
          <Button
            className={`w-full ${
              plan.popular
                ? 'bg-gradient-to-r from-blue-500 to-indigo-500 hover:from-blue-600 hover:to-indigo-600 shadow-lg shadow-indigo-500/25'
                : 'bg-gray-900 hover:bg-gray-800'
            }`}
            size="lg"
          >
            {plan.cta}
          </Button>
        </Link>
      </CardContent>
    </Card>
  );
}

export function PricingTable() {
  const [mode, setMode] = useState<BillingMode>('individual');
  const plans = mode === 'individual' ? INDIVIDUAL_PLANS : TEAM_PLANS;

  return (
    <div>
      <BillingModeToggle mode={mode} onChange={setMode} />
      <div className="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {plans.map((plan) => (
          <PricingCard key={plan.planId} plan={plan} mode={mode} />
        ))}
      </div>

      {mode === 'individual' && (
        <p className="text-center text-sm text-gray-500 mt-8">
          Individual subscriptions work across all your Slack workspaces
        </p>
      )}
      {mode === 'team' && (
        <p className="text-center text-sm text-gray-500 mt-8">
          Team pricing requires admin access to install for your organization
        </p>
      )}
    </div>
  );
}
```

Key design decisions:
- Individual plans show /month, team plans show /seat/month
- CTA links include mode parameter for checkout routing
- Individual plan IDs prefixed with 'individual_' for clear distinction
- Helpful subtext explains the difference between modes
- Component is client-side for toggle state
  </action>
  <verify>
Run `npm run typecheck --workspace=web-portal` to verify types.
Run `npm run dev --workspace=web-portal` and check /pricing page manually.
  </verify>
  <done>Pricing table shows billing mode toggle, individual plans with /month pricing, team plans with /seat/month pricing.</done>
</task>

<task type="auto">
  <name>Task 2: Update pricing page metadata</name>
  <files>apps/web-portal/app/pricing/page.tsx</files>
  <action>
Update the pricing page to reflect individual billing option:

1. Update metadata description:

```typescript
export const metadata: Metadata = {
  title: 'Pricing - Speak for Me',
  description:
    'Simple, transparent pricing for AI-powered Slack response suggestions. Individual plans from $10/month, team plans from $10/seat/month. Start with a 14-day free trial.',
  openGraph: {
    title: 'Pricing - Speak for Me',
    description:
      'AI-powered Slack response suggestions. Individual and team plans with 14-day free trial.',
    type: 'website',
  },
};
```

2. Update hero subheadline to mention both options:

```tsx
<p
  id="pricing-subheadline"
  className="mt-4 text-lg text-gray-600 max-w-2xl mx-auto"
>
  Start with a 14-day free trial. Choose individual or team billing.
</p>
```

3. Ensure the CTA in the hero section links appropriately (the main "Add to Slack" already goes to /slack/install which is correct for both modes - they select mode on pricing table).

No other changes needed - PricingTable component handles the rest.
  </action>
  <verify>
Run `npm run typecheck --workspace=web-portal` to verify types.
Verify metadata is updated.
  </verify>
  <done>Pricing page metadata updated to reflect individual and team options.</done>
</task>

</tasks>

<verification>
1. Pricing page shows billing mode toggle (For Myself / For My Team)
2. Toggle switches between individual and team plan cards
3. Individual plans show /month, team plans show /seat/month
4. CTA links include mode and plan parameters
5. Subtext explains mode differences
</verification>

<success_criteria>
- BillingModeToggle renders and switches between modes
- Individual mode shows Starter ($10/month) and Pro ($15/month)
- Team mode shows Team Starter ($10/seat/month) and Team Pro ($15/seat/month)
- Links to /login include plan and mode query params
- Page metadata reflects both pricing options
</success_criteria>

<output>
After completion, create `.planning/phases/11-individual-billing/11-04-SUMMARY.md`
</output>
